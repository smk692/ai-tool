# SpecKit 활용 가이드

SpecKit은 기능 명세서 기반 개발 워크플로우를 지원하는 도구입니다.

---

## 1. 빠른 시작 (Quick Start)

### 새 기능 시작하기

```bash
# 1. 현재 작업 저장 (중요!)
git add . && git commit -m "WIP: 현재 작업 저장"

# 2. master 브랜치로 이동
git checkout master
git pull origin master

# 3. 스펙 작성 시작 (브랜치 + 폴더 자동 생성!)
# Claude에게:
/speckit.specify Slack RAG 챗봇

# ↑ 이 명령어가 자동으로:
#   - 기존 브랜치 번호 확인 → 005 계산
#   - 005-rag-chatbot 브랜치 생성 및 체크아웃
#   - specs/005-rag-chatbot/ 폴더 생성
#   - spec.md 템플릿 복사 및 명세 작성
```

> **💡 참고**: `/speckit.specify`가 내부적으로 `create-new-feature.sh` 스크립트를 호출합니다.
> 수동으로 브랜치를 만들 필요가 없습니다!

### 기존 기능 이어하기

```bash
# 1. 현재 작업 저장 (⚠️ 필수!)
git add . && git commit -m "WIP: 현재 작업 저장"

# 2. 기존 기능 브랜치로 전환
git checkout 005-rag-chatbot

# 3. 최신 상태 확인
git status
ls specs/005-rag-chatbot/

# 4. 작업 이어하기
# Claude에게: /speckit.plan 또는 /speckit.implement
```

### 동시에 여러 기능 작업하기 (워크트리)

```bash
# 별도 디렉토리에 다른 브랜치 체크아웃
git worktree add ../ai-tool-005 005-rag-chatbot

# 이제 두 폴더에서 독립적으로 작업 가능
# - ai-tool/        → 004-rag-indexer 브랜치
# - ai-tool-005/    → 005-rag-chatbot 브랜치

# 워크트리 목록 확인
git worktree list

# 워크트리 제거 (작업 완료 후)
git worktree remove ../ai-tool-005
```

---

## 2. 전체 워크플로우

```
┌─────────────────────────────────────────────────────────────────────┐
│  새 기능                                                             │
│    │                                                                │
│    ▼                                                                │
│  /speckit.specify  ──→  spec.md (기능 명세서)                        │
│    │                                                                │
│    ▼                                                                │
│  /speckit.clarify  ──→  spec.md 업데이트 (불명확한 부분 명확화)        │
│    │                                                                │
│    ▼                                                                │
│  /speckit.checklist ──→ checklists/requirements.md (품질 검증)       │
│    │                                                                │
│    ▼                                                                │
│  /speckit.plan     ──→  plan.md (구현 계획)                          │
│    │                                                                │
│    ▼                                                                │
│  /speckit.tasks    ──→  tasks.md (실행 가능한 작업 목록)              │
│    │                                                                │
│    ▼                                                                │
│  /speckit.analyze  ──→  품질 분석 (선택사항)                          │
│    │                                                                │
│    ▼                                                                │
│  /speckit.implement ──→ 코드 구현                                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 명령어 레퍼런스

| 명령어 | 설명 | 산출물 | 언제 사용? |
|--------|------|--------|-----------|
| `/speckit.specify` | 기능 명세 생성 | `spec.md` | 새 기능 시작 시 |
| `/speckit.clarify` | 명세 명확화 | `spec.md` 업데이트 | 요구사항이 불명확할 때 |
| `/speckit.checklist` | 품질 체크리스트 | `checklists/requirements.md` | 명세 검증 시 |
| `/speckit.plan` | 구현 계획 수립 | `plan.md` | 명세 완료 후 |
| `/speckit.tasks` | 작업 목록 생성 | `tasks.md` | 계획 완료 후 |
| `/speckit.analyze` | 품질 분석 | 분석 리포트 | 구현 전 검증 |
| `/speckit.implement` | 구현 실행 | 코드 | tasks.md 완료 후 |

### 사용 예시

```
# 새 기능 명세 작성
/speckit.specify 사용자 알림 기능을 만들고 싶어.
매일 오전 9시에 일일 리포트를 Slack으로 발송해야 해.

# 기존 명세에 내용 추가
/speckit.specify 기존 spec.md에 에러 처리 시나리오 추가해줘

# 명확화 질문
/speckit.clarify

# 구현 계획
/speckit.plan

# 작업 목록 생성
/speckit.tasks

# 구현 시작
/speckit.implement
```

---

## 4. 브랜치 전략 ⚠️ 중요

### 핵심 규칙

| 규칙 | 설명 | 왜? |
|------|------|-----|
| **규칙 1** | 기능당 하나의 브랜치 | `specs/` 폴더가 Git으로 추적됨 |
| **규칙 2** | 브랜치 전환 전 반드시 커밋 | 미커밋 변경사항이 다른 브랜치로 영향 |
| **규칙 3** | 동시 작업은 워크트리 사용 | 브랜치 전환 없이 독립 작업 가능 |

### 올바른 작업 흐름

```bash
# ✅ 올바른 예시
git checkout 004-rag-indexer
# ... 004 작업 ...
git add . && git commit -m "feat(004): 인덱서 구현"
git checkout 005-rag-chatbot
# ... 005 작업 ...

# ❌ 잘못된 예시 (specs 폴더 충돌 발생!)
git checkout 004-rag-indexer
# ... 004 작업 (커밋 안함) ...
git checkout 005-rag-chatbot  # 004 변경사항이 005로 영향!
```

### 브랜치 네이밍 규칙

```
{번호}-{기능명}

예시:
- 001-claude-huggingface-migration
- 002-embedding-validation
- 004-rag-indexer
- 005-rag-chatbot
```

---

## 5. 주의사항 & 트러블슈팅

### specs 폴더 충돌 방지

```bash
# 브랜치 전환 전 체크리스트
git status                    # 변경사항 확인
git add specs/                # specs 폴더 스테이징
git commit -m "WIP: specs 저장"  # 커밋
git checkout {다른브랜치}      # 안전하게 전환
```

### 실수로 덮어씌워진 경우 복구

```bash
# 최근 커밋으로 복구
git checkout HEAD -- specs/{기능폴더}/

# 특정 커밋으로 복구
git log --oneline specs/{기능폴더}/  # 커밋 이력 확인
git checkout {커밋해시} -- specs/{기능폴더}/

# stash에서 복구
git stash list
git stash pop
```

### 워크트리 문제 해결

```bash
# 워크트리 상태 확인
git worktree list

# 잠긴 워크트리 강제 제거
git worktree remove --force ../ai-tool-005

# 워크트리 정리 (삭제된 디렉토리 정리)
git worktree prune
```

---

## 6. 폴더 구조

```
ai-tool/
├── .specify/                    # SpecKit 설정
│   ├── memory/
│   │   └── constitution.md     # 프로젝트 헌법 (핵심 원칙)
│   ├── scripts/
│   │   └── bash/
│   │       └── create-new-feature.sh  # 새 기능 생성 스크립트
│   └── templates/
│       ├── spec-template.md    # 명세서 템플릿
│       ├── plan-template.md    # 계획서 템플릿
│       └── tasks-template.md   # 작업 목록 템플릿
│
├── specs/                       # 기능별 명세 폴더
│   ├── 004-rag-indexer/
│   │   ├── spec.md             # 기능 명세서
│   │   ├── plan.md             # 구현 계획
│   │   ├── tasks.md            # 작업 목록
│   │   └── checklists/
│   │       └── requirements.md # 요구사항 체크리스트
│   │
│   └── 005-rag-chatbot/
│       ├── spec.md
│       └── checklists/
│           └── requirements.md
│
├── rag-indexer/                 # 실제 구현 코드
│   ├── src/
│   └── tests/
│
└── rag-chatbot/                 # 실제 구현 코드
    ├── src/
    └── tests/
```

---

## 7. 자주 묻는 질문 (FAQ)

### Q: 브랜치를 수동으로 만들어야 하나요?
**아니요!** `/speckit.specify` 명령어가 자동으로:
- 브랜치 번호 계산 (기존 브랜치 확인)
- 새 브랜치 생성 및 체크아웃
- specs 폴더 및 템플릿 생성

수동으로 스크립트를 실행하려면:
```bash
.specify/scripts/bash/create-new-feature.sh "기능 설명"
```

### Q: 명세서만 작성하고 구현은 나중에 하고 싶어요
```bash
# 명세 완료 후 커밋하고 다른 작업으로 전환
/speckit.specify 새 기능 설명  # 브랜치 자동 생성됨
/speckit.checklist
git add specs/005-rag-chatbot/
git commit -m "docs(005): spec 및 checklist 완료"
git checkout 004-rag-indexer  # 다른 작업으로 전환
```

### Q: 여러 기능을 동시에 구현하고 싶어요
```bash
# 워크트리 사용 (권장)
git worktree add ../ai-tool-004 004-rag-indexer
git worktree add ../ai-tool-005 005-rag-chatbot

# 각 폴더에서 독립적으로 Claude Code 실행
cd ../ai-tool-004 && claude
cd ../ai-tool-005 && claude
```

### Q: 이전 기능의 spec을 참고하고 싶어요
```bash
# 다른 브랜치의 파일 읽기 (현재 브랜치 변경 없이)
git show 004-rag-indexer:specs/004-rag-indexer/spec.md
```

---

## 8. 체크리스트

### 새 기능 시작 전
- [ ] 현재 작업 커밋 완료
- [ ] master 브랜치 최신화
- [ ] 새 브랜치 생성

### 브랜치 전환 전
- [ ] `git status`로 변경사항 확인
- [ ] `git add .` → `git commit` 완료

### 작업 완료 후
- [ ] 모든 테스트 통과
- [ ] specs/ 폴더 최신 상태 커밋
- [ ] PR 생성 또는 master 머지

---

**Last Updated**: 2025-12-10
